# Write your MySQL query statement below
SELECT q.query_name , ROUND(SUM(q.rating/q.position)/(COUNT(*)),2)  AS quality , ROUND(SUM(q.rating<3)*100/(COUNT(*)),2) AS poor_query_percentage from Queries q GROUP BY query_name;